---
title: All Books
layout: layout.njk
---

<section class="max-w-5xl mx-auto">
  <h1 class="text-4xl font-serif mb-10">Books</h1>

  <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-3">
    {#
      Sort by order, then loop.
      (If you ever mix series, filter by series first.)
    #}
    {% for book in books | sort(attribute='order') %}
      <article
        class="rounded-2xl shadow-lg overflow-hidden flex flex-col"
        style="--accent: {{ book.accent }}"
      >
        <img src="{{ book.cover }}" alt="{{ book.title }} cover">

        <div class="p-6 grow flex flex-col">
          <h2 class="font-serif text-2xl text-accent mb-2">
            {{ book.title }}
          </h2>

          <p class="text-sm opacity-80 mb-4">
            {{ book.series }} · {{ book.badge }}
          </p>

          <p class="mb-4 grow">{{ book.blurb }}</p>

          <a href="/{{ book.slug }}/" class="mt-auto inline-block 
                     bg-accent/90 hover:bg-accent text-white px-4 py-2 rounded">
            Read more →
          </a>
        </div>
      </article>
    {% endfor %}
  </div>
</section>